Please analyze the following {{ domain_type }} text using X-bar theory and identify ALL applicable spans at every hierarchical level.

**Text to analyze:**
```
{{ text }}
```

**Analysis Requirements:**
1. Identify spans at EVERY level: {{ levels | join(", ") }}
2. Use the {{ domain_type }} domain classifiers provided in the system prompt
3. Provide comprehensive coverage within the span limit: 
   - Prioritize the most important linguistic elements
   - Focus on key words, phrases, and structural boundaries
   - Don't skip critical functional elements
4. Include confidence scores based on how certain you are about each classification
5. Ensure spans are hierarchically consistent (larger spans contain smaller spans)

**Expected classifier types for this domain:**
{{ expected_classifiers | join(", ") }}

{% set word_count = text.split() | length %}
{% set max_spans = max_spans_per_sequence|default(64) %}
{% if domain_type == "natural" %}
**Target span generation**: For {{ word_count }} words, generate up to {{ max_spans }} high-quality spans focusing on key linguistic structures
{% elif domain_type == "code" %}
**Target span generation**: For {{ word_count }} words, generate up to {{ max_spans }} high-quality spans focusing on key code structures
{% else %}
**Target span generation**: For {{ word_count }} words, generate up to {{ max_spans }} high-quality spans focusing on key mixed content elements
{% endif %}

**IMPORTANT LIMITS**:
- **Maximum {{ max_spans }} spans per sequence** - Focus on quality over quantity
- Prioritize the most important linguistic structures first
- Each span should provide clear linguistic value

**Critical Instructions:**
- **Quality over quantity** - Focus on the most important {{ max_spans }} spans
- Prioritize: key phrases, clause boundaries, important words
- Analyze systematically but selectively 
- Each span should contribute meaningful linguistic structure
- **Maximum {{ max_spans }} spans in your JSON response**

Return your analysis as a JSON array with each span containing:
- `text`: The exact text of the span (I will calculate positions automatically)
- `xbar_class`: The X-bar classifier name (use full names like "determiner", "noun", "verb_phrase")  
- `confidence`: Confidence score (0.0-1.0)

**IMPORTANT**: Do NOT provide start_char/end_char positions. Just provide the exact text snippets and I will find their positions automatically. This ensures accurate boundary detection.

{% if turn_number > 1 %}
**Follow-up Analysis:**
This is turn {{ turn_number }} of {{ max_turns }}. Please review your previous analysis and:
- Add any spans you may have missed (check every word!)
- Refine confidence scores
- Ensure complete hierarchical coverage
- Focus on {{ focus_area }} if specified
- Make sure you haven't missed any verbs, prepositions, or function words
{% endif %}

Begin your comprehensive X-bar analysis now:
